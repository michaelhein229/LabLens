EMBEDDING_MODEL_NAME = "all-MiniLM-L6-v2"

expected_metadata = {
    "embedding_model": EMBEDDING_MODEL_NAME,
    "embedding_dimension": 384,
    "index_schema_version": 1,
    "chunking_strategy": "one-slide-per-chunk",
}